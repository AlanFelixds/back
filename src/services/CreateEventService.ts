import prismaClient from "./prisma";


class CreateEventService {

    async execute(name: string, date: Date){

        // const createEvent = await prismaClient.events.create({
        //     data: {
        //         name,
        //         date,

                
        //     }
        // })

    }
}